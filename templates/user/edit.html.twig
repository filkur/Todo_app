{% extends 'base.html.twig' %}

{% block title %}Todo-app! {% endblock %}

{% block body %}

    <div class="container" style="margin-top: 50px;">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        <div class="row">
            <div class="col-sm">
                <p><h5>Username: {{ app.user.username }} <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#editUsername">edit
                        <i class="icon-pencil"></i>
                    </button> </h5></p>
                <p style="margin-top: 50px;"><h5>Email: {{ app.user.email }} <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#editEmail">edit
                        <i class="icon-pencil"></i>
                    </button></h5></p>
                <button style="margin-top: 50px;" type="button" class="btn btn-danger" data-toggle="modal" data-target="#exampleModalCenter">
                    Remove account
                </button>

            </div>
            <div class="col-sm">
                <p><h5>Profile image: <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#editImage">edit
                        <i class="icon-pencil"></i>
                    </button></h5></p>
                <img src="{{ asset('images/profile/' ~ app.user.image) }}" style="width: 300px" alt="profile-image">
            </div>

        </div>
    </div>

    <!-- Delete modal -->
    <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Are you sure?</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    all your data will be deleted along with created and completed categories, tasks!
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-danger btn-md" href="{{ path('user_delete', {id: app.user.id})}}">Remove</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Username edit modal -->
    <div class="modal fade" id="editUsername" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Edit</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{form(formUsername)}}
                </div>
            </div>
        </div>
    </div>
    <!-- email edit modal -->
    <div class="modal fade" id="editEmail" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Edit</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{form(formEmail)}}
                </div>
            </div>
        </div>
    </div>
    <!-- Username edit modal -->
    <div class="modal fade" id="editImage" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Edit</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{form(formImage)}}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
